# Drop the unused legacy adapter

Bad input reached storage and failed there, leaving partial state. Validation now happens before any write.

Change #6 of 6 on branch `pr/20260811-105826-6-drop-the-unused-legacy-adapter`.
