# Cache lookups in the resolver hot path

Bad input reached storage and failed there, leaving partial state. Validation now happens before any write.

Change #4 of 6 on branch `pr/20260811-105826-4-cache-lookups-in-the-resolver-hot-path`.
